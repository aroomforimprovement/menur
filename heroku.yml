build:
    docker:
        web: Dockerfile
run:
#    web: /bin/bash -c "envsubst '\$PORT' < /conf.d/default.conf.template > /etc/nginx/conf.d/default" && nginx -g daemon off;
    web:  nginx -g daemon off;