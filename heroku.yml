build:
    #config:
    #    PORT
    docker:
        web: Dockerfile
run:
#    web: /bin/bash -c "envsubst '\$PORT' < /conf.d/nginx.conf > /etc/nginx/conf.d/nginx.conf" && nginx -g daemon off;
    web:  nginx -g "daemon off;"